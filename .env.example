# FastAPI backend base URL (public, safe to expose in frontend)
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Admin email used to gate /admin pages in the frontend
NEXT_PUBLIC_ADMIN_EMAIL=admin@example.com
