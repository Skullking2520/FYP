# Git LFS rules scoped to backend only

app/ml_assets/job_recommender_fast.pkl filter=lfs diff=lfs merge=lfs -text

# NLP model artifacts (fine-tuned checkpoints / adapters)
app/nlp/**/*.safetensors filter=lfs diff=lfs merge=lfs -text
app/nlp/**/*.pt filter=lfs diff=lfs merge=lfs -text
app/nlp/**/*.pth filter=lfs diff=lfs merge=lfs -text
